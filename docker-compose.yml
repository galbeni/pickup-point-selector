services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_GRAPHQL_ENDPOINT: ${NEXT_PUBLIC_GRAPHQL_ENDPOINT}
        NEXT_PUBLIC_MERCHANT_ID: ${NEXT_PUBLIC_MERCHANT_ID}
        NEXT_PUBLIC_SESSION_ID: ${NEXT_PUBLIC_SESSION_ID}
        NEXT_PUBLIC_DEFAULT_PAGE_SIZE: ${NEXT_PUBLIC_DEFAULT_PAGE_SIZE}
    ports:
      - "3000:3000"
